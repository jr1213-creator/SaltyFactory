import { NextResponse } from 'next/server'; import { requireStudioUser, getAuditActor } from '@saltyfactory/auth'; import { fixtures, evaluatePublishReviewGates } from '@saltyfactory/domain'; export async function GET(){ await requireStudioUser(); return NextResponse.json({ok:true, route:'mockups/generate', auditActor: await getAuditActor(), gates:evaluatePublishReviewGates(fixtures.publishReviewBlocked)}); } export async function POST(){ await requireStudioUser(); return NextResponse.json({ok:true, route:'mockups/generate', auditEvent:'created'}); }
