import { parseEnv, redactConfig } from "@saltyfactory/config"; export default function Page(){ const cfg=redactConfig(parseEnv()); return <><h1>Studio Dashboard</h1><section className="card"><h2>Pipeline Summary</h2><p>Trend, phrase, brief, generation, QA, mockup, draft, publish, and fulfillment queues are guarded by server-side actions and audit events.</p></section><pre className="card">{JSON.stringify(cfg.providers,null,2)}</pre></>; }
