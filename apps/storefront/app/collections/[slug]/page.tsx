import { getProducts } from "../../../src/data"; export default async function Page({params}:{params:Promise<{slug:string}>}){ const {slug}=await params; const products=await getProducts(); return <main className="wrap"><h1>{slug}</h1><div className="grid">{products.map((p:any)=><article className="card" key={p.id}><h2>{p.title}</h2><a href={`/products/${p.handle}`}>View</a></article>)}</div></main>; }
