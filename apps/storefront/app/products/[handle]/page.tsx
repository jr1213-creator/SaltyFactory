import { getProduct } from "../../../src/data"; import { structuredDataProductProjection } from "@saltyfactory/commerce"; type Product={title?:unknown;description?:unknown}; export default async function Page({params}:{params:Promise<{handle:string}>}){ const {handle}=await params; const product=await getProduct(handle) as Product | null; if(!product) return <main className="wrap"><h1>Product unavailable</h1></main>; const title=String(product.title ?? "Product"); const description=String(product.description ?? ""); return <main className="wrap"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(structuredDataProductProjection({...product,title,description}))}}/><h1>{title}</h1><p>{description}</p><select><option>Ivory / M</option></select><button className="btn">Add to cart</button></main>; }
