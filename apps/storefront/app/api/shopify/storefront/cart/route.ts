import { NextResponse } from 'next/server'; import { parseEnv } from '@saltyfactory/config'; import { createCommerceProviders } from '@saltyfactory/commerce'; export async function GET(){ const p=createCommerceProviders(parseEnv()).storefront; const data= await (p as any).getProducts?.(); return NextResponse.json(data); } export async function POST(){ return NextResponse.json({ok:false,error:'cart_action_unavailable_when_provider_disabled'},{status:200}); }
