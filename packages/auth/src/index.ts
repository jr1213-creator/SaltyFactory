export type StudioUser={id:string;email:string;role:"owner"|"admin"}; export async function getStudioUser():Promise<StudioUser|null>{return process.env.STUDIO_ADMIN_EMAIL?{id:process.env.STUDIO_ADMIN_EMAIL,email:process.env.STUDIO_ADMIN_EMAIL,role:"owner"}:null} export async function requireStudioUser(){if(process.env.NODE_ENV==="production"&&process.env.STUDIO_AUTH_ENABLED==="false")throw new Error("Production auth bypass forbidden");const u=await getStudioUser();if(!u)throw Object.assign(new Error("unauthenticated"),{status:401});return u} export async function requireStudioAdmin(){return requireStudioUser()} export async function getAuditActor(){const u=await getStudioUser();return u?{actor_type:"human" as const,actor_id:u.id}:null} export async function requireAuditActor(){const actor=await getAuditActor(); if(!actor?.actor_id) throw Object.assign(new Error("audit_actor_required"),{status:401}); return actor} export const loginCookie=(email:string)=>`studio_email=${encodeURIComponent(email)}; Path=/; HttpOnly; SameSite=Lax`; export const logoutCookie=()=>"studio_email=; Path=/; HttpOnly; Max-Age=0; SameSite=Lax";
