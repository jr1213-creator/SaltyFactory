import type { RiskReview } from "@saltyfactory/domain"; const terms={trademark_risk:["nike","barbie","dutton"],celebrity_reference:["taylor swift","beyonce"],sports_team_reference:["cowboys","lakers"],brand_lookalike:["just do it"],disney_ip_risk:["disney","mickey"],music_artist_reference:["taylor swift"],tv_show_reference:["yellowstone"],college_reference:["university of"],nfl_nba_reference:["nfl","nba","ncaa"],profanity_flag:["fuck","shit"],competitor_copy_flag:["etsy bestseller"]}; export type RiskChecks=Record<keyof typeof terms,boolean>; export const riskCategories=Object.keys(terms); export function detectRisk(text:string):RiskChecks{const l=text.toLowerCase();return Object.fromEntries(Object.entries(terms).map(([k,v])=>[k,v.some(x=>l.includes(x))])) as RiskChecks} export const calculateRiskScore=(c:RiskChecks)=>Math.min(1,Object.values(c).filter(Boolean).length/3); export const requiresHumanReview=(c:RiskChecks)=>Object.values(c).some(Boolean); export const trademarkReviewRequired=(t:string)=>detectRisk(t).trademark_risk||/coastal cowgirl|beach rodeo|salty/i.test(t); export function reviewPhraseRisk(entity_id:string,phrase:string):RiskReview{const checks=detectRisk(phrase),risk_score=calculateRiskScore(checks),flag=requiresHumanReview(checks);return{id:`risk_${Date.now()}`,entity_type:"phrase_candidate",entity_id,checks,risk_score,status:flag?"auto_flagged":"cleared",reviewed_by:flag?null:"system",reviewer_id:null,reviewed_at:flag?null:new Date().toISOString(),notes:"Risk flagging only; not legal clearance.",created_at:new Date().toISOString()}} export const riskBlocksPublish=(r:RiskReview)=>r.status!=="cleared"||r.risk_score>=.67;