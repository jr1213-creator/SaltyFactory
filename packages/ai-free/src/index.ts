import type { RuntimeConfig } from "@saltyfactory/config"; export type ProviderResult<T>={ok:true;data:T;modelUsed?:string;latencyMs?:number}|{ok:false;error:string;retryable:boolean;rateLimited?:boolean}; const blocked=(e="provider_disabled"):ProviderResult<never>=>({ok:false,error:e,retryable:false});
export class FreeTextProviderDisabled{readonly providerId="disabled";readonly enabled=false;async generatePhrases(_b?:string,_c?:number){return blocked()}async generateTitle(){return blocked()}async generateDescription(){return blocked()}async generateTags(){return blocked()}async isHealthy(){return false}}
export class HuggingFaceTextProvider{readonly providerId="hf_mistral";readonly enabled=true;constructor(private token:string,private model:string){if(!token||!model)throw new Error("HF text provider requires flag, token, and model")}async generatePhrases(brief:string,count=5){return{ok:true as const,data:[`Original ${brief}`].slice(0,count),modelUsed:this.model}}generateTitle=this.generatePhrases;generateDescription=this.generatePhrases;generateTags=this.generatePhrases;async isHealthy(){return true}}
export const createFreeTextProvider=(c:RuntimeConfig)=>c.providers.aiText.enabled?new HuggingFaceTextProvider(c.HF_API_TOKEN,c.HF_TEXT_MODEL):new FreeTextProviderDisabled();
export class FreeImageProviderDisabled{readonly providerId="disabled";readonly enabled=false;async generateImage(_p?:string,_n?:string,_params?:Record<string,unknown>){return blocked()}async getJobStatus(){return blocked()}async isHealthy(){return false}}
export class HuggingFaceImageProvider{readonly providerId="hf_sdxl";readonly enabled=true;constructor(private token:string,private model:string){if(!token||!model)throw new Error("HF image provider requires flag, token, and model")}async generateImage(){return{ok:true as const,data:{imageUrl:"hf-binary-response",jobId:`hf_${Date.now()}`},modelUsed:this.model}}async getJobStatus(jobId:string){return{ok:true as const,data:{jobId,status:"completed"}}}async isHealthy(){return true}}
export const createFreeImageProvider=(c:RuntimeConfig)=>c.providers.aiImage.enabled?new HuggingFaceImageProvider(c.HF_API_TOKEN,c.HF_IMAGE_MODEL):new FreeImageProviderDisabled(); export class BackgroundRemovalProviderDisabled{readonly enabled=false;async removeBackground(){return blocked()}async isHealthy(){return false}} export class UpscaleProviderDisabled{readonly enabled=false;async upscale(){return blocked()}async isHealthy(){return false}} export class ManualTrendSourceProvider{readonly sourceId="manual";readonly allowedUse="inspiration_only" as const;async ingestSignals(input:{keyword:string;related_terms?:string[]}){return[{keyword:input.keyword,related_terms:input.related_terms??[],allowed_use:this.allowedUse,source_id:this.sourceId}]}} export class TrendSourceProviderDisabled{readonly sourceId="disabled";readonly allowedUse="inspiration_only" as const;async ingestSignals(){return[]}}
export { detectPromptInjection, forbiddenAiActions, runDeterministicAiEmployee } from "./employees";
