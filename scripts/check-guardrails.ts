import { readdirSync, readFileSync, statSync } from "node:fs"; import { join } from "node:path";
const root=process.cwd(); const files:string[]=[]; function walk(d:string){ for(const e of readdirSync(d)){ if(["node_modules",".next","dist"].includes(e)) continue; const p=join(d,e); const s=statSync(p); if(s.isDirectory()) walk(p); else if(/\.(ts|tsx|js|json|env)$/.test(e)) files.push(p); }} walk(root);
const text=(p:string)=>readFileSync(p,"utf8"); const pkg=JSON.parse(text(join(root,"package.json"))); const deps=JSON.stringify({...pkg.dependencies,...pkg.devDependencies}).toLowerCase(); const failures:string[]=[]; if(deps.includes("openai")) failures.push("OpenAI dependency found"); if(deps.includes("anthropic")) failures.push("Anthropic dependency found");
for(const f of files){ const t=text(f); const publicSecret = /NEXT_PUBLIC_(SHOPIFY_ADMIN_TOKEN|PRINTIFY_API_TOKEN|SUPABASE_SERVICE_ROLE_KEY|HF_API_TOKEN|REPLICATE_API_TOKEN|REMOVE_BG_API_KEY)/.test(t); if(publicSecret) failures.push(`${f}: secret-like NEXT_PUBLIC exposure`); const normalized=f.replace(/\\/g,"/"); if(/app\/api\/(generate|publish)/.test(normalized) && !/app\/api\/studio\//.test(normalized)) failures.push(`${f}: public generation or publish route`); }
const env=text(join(root,".env.example")); for(const bad of ["OPENAI","ANTHROPIC"]) if(env.includes(bad)) failures.push(`${bad} env var documented`); for(const line of ["AI_TEXT_ENABLED=false","AI_IMAGE_ENABLED=false","LIVE_PUBLISHING_ENABLED=false","SHOPIFY_ADMIN_ENABLED=false","PRINTIFY_ENABLED=false"]) if(!env.includes(line)) failures.push(`Missing default ${line}`); for(const needle of ["evaluatePublishReviewGates","AuditEventRepository","startStudioMagicLink","exchangeSupabaseAuthCode","SUPABASE_ACCESS_COOKIE"]) if(!files.some(f=>text(f).includes(needle))) failures.push(`Missing ${needle}`); const authText=text(join(root,"packages/auth/src/index.ts")); const proxyText=text(join(root,"apps/studio/proxy.ts")); const loginRoute=text(join(root,"apps/studio/app/api/studio/login/route.ts")); if(/Boolean\(process\.env\.STUDIO_ADMIN_EMAIL\)/.test(proxyText)||/return process\.env\.STUDIO_ADMIN_EMAIL\s*\?/.test(authText)||/createStudioSessionValue|verifyStudioSessionValue|sf_studio_session/.test(authText)) failures.push("Studio auth must use Supabase Auth, not env-only or custom signed sessions"); if(/STUDIO_ADMIN_PASSWORD_HASH|PASSWORD_HASH|bcrypt|argon2/.test(authText+env)) failures.push("Custom password hash auth is forbidden"); if(!/pathname === "\/api\/studio\/login"/.test(proxyText)) failures.push("Studio login API must remain public in proxy"); if(!/signInWithOtp/.test(authText)||!/exchangeCodeForSession/.test(authText)||!/getUser\(accessToken\)/.test(authText)) failures.push("Supabase Auth OTP/callback/session verification missing"); if(/Set-Cookie/.test(loginRoute)) failures.push("Studio login route must not create sessions directly"); const route=(p:string)=>text(join(root,"apps/studio/app/api/studio",p,"route.ts")); for(const p of ["publish/shopify","publish/printify"]){ const t=route(p); const post=t.split("export async function POST")[1]||""; if(!t.includes("requirePublishPermission")) failures.push(`${p}: publish route must require publish permission`); if(/requireStudioUser\s*\(/.test(t)||/requireAuditActor\s*\(/.test(t)) failures.push(`${p}: publish route uses generic auth`); if(!post.includes("evaluatePublishReviewGates")) failures.push(`${p}: publish POST gate evaluator missing`); if(/ok:\s*true/.test(post)) failures.push(`${p}: publish POST must not return fixture-only success`); if(!/not_implemented|provider_disabled/.test(post)) failures.push(`${p}: publish POST must fail honestly until persisted provider flow exists`); } const approveRoute=route("drafts/approve"); if(!approveRoute.includes("requireApprovalPermission")) failures.push("drafts/approve: approval route must require approval permission"); if(/requireStudioUser\s*\(|requireAuditActor\s*\(/.test(approveRoute)) failures.push("drafts/approve: approval route uses generic auth"); for(const p of ["generate/submit","generate/status","mockups/generate","phrases/generate","trends/ingest"]){ const t=route(p); if(!t.includes("requireProviderMutationPermission")) failures.push(`${p}: provider-impacting route must require provider mutation permission`); } for(const f of files.filter(f=>f.replace(/\\/g,"/").includes("apps/studio/app/api/studio/"))){ const t=text(f); if(/body\.(role|user_id|userId|organization_id|organizationId|workspace_id|workspaceId)/.test(t)) failures.push(`${f}: Studio API route appears to trust client-provided identity/workspace fields`); } const publicServiceRoleName="NEXT_PUBLIC_SUPABASE_"+"SERVICE_ROLE_KEY"; if((env+files.map(f=>text(f)).join("\n")).includes(publicServiceRoleName)) failures.push("Service role key must never be public"); if(failures.length){ console.error(failures.join("\n")); process.exit(1);} console.log("guardrails passed");
