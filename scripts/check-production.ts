import { existsSync, readFileSync, readdirSync, statSync } from "node:fs"; import { join } from "node:path"; import { parseEnv, validateProductionReadiness } from "../packages/config/src/index"; import { selectRepositoryAdapter } from "../packages/db/src/repositories/factory";
function studioRoute(path:string){ return readFileSync(join("apps","studio","app","api","studio",path,"route.ts"),"utf8"); }
function walkRoutes(dir:string,out:string[]=[]){ for(const entry of readdirSync(dir)){ const path=join(dir,entry); const stat=statSync(path); if(stat.isDirectory()) walkRoutes(path,out); else if(entry==="route.ts") out.push(path); } return out; }
const failures:string[]=[]; const env=readFileSync(".env.example","utf8"); for(const key of ["DATABASE_URL","SUPABASE_SERVICE_ROLE_KEY","NEXT_PUBLIC_SUPABASE_URL","NEXT_PUBLIC_SUPABASE_ANON_KEY","STUDIO_AUTH_ENABLED=true","LIVE_PUBLISHING_ENABLED=false","REPOSITORY_ADAPTER=auto","CREDENTIAL_STORAGE_ENABLED=false","CREDENTIAL_ENCRYPTION_KEY"]) if(!env.includes(key)) failures.push(`.env.example missing ${key}`); const migrations=existsSync("packages/db/migrations")?readdirSync("packages/db/migrations").filter(f=>/^0000_.*\.sql$/.test(f)):[]; if(!migrations.length) failures.push("base migration missing"); else { const sql=readFileSync(`packages/db/migrations/${migrations[0]}`,"utf8"); for(const needle of ['CREATE TABLE "trend_signals"','"workspace_id" text NOT NULL','"gates" jsonb','"secret_ref" text']) if(!sql.includes(needle)) failures.push(`base migration missing ${needle}`); } const disabled=parseEnv({APP_ENV:"production",NODE_ENV:"production",STUDIO_AUTH_ENABLED:"true",AI_TEXT_ENABLED:"false",AI_IMAGE_ENABLED:"false",BACKGROUND_REMOVAL_ENABLED:"false",UPSCALE_ENABLED:"false",SHOPIFY_STOREFRONT_ENABLED:"false",SHOPIFY_ADMIN_ENABLED:"false",PRINTIFY_ENABLED:"false",LIVE_PUBLISHING_ENABLED:"false",NEXT_PUBLIC_STOREFRONT_BASE_URL:"https://saltycowhide.com"}); if(disabled.providers.aiText.enabled) failures.push("AI text enabled without token"); const readiness=validateProductionReadiness(disabled); if(!readiness.failures.includes("DATABASE_URL required for production managed Postgres")) failures.push("production readiness did not require DATABASE_URL"); if(!readiness.failures.includes("Supabase URL and service role key required server-side")) failures.push("production readiness did not require Supabase server config"); if(!readiness.failures.includes("Supabase Auth URL required for Studio auth")) failures.push("production readiness did not require Supabase Auth URL"); if(!readiness.failures.includes("Supabase anon key required for Studio auth")) failures.push("production readiness did not require Supabase anon key"); const credentialReadiness=validateProductionReadiness(parseEnv({APP_ENV:"production",NODE_ENV:"production",STUDIO_AUTH_ENABLED:"true",CREDENTIAL_STORAGE_ENABLED:"true",DATABASE_URL:"postgres://example",SUPABASE_URL:"https://example.supabase.co",SUPABASE_SERVICE_ROLE_KEY:"service",NEXT_PUBLIC_SUPABASE_URL:"https://example.supabase.co",NEXT_PUBLIC_SUPABASE_ANON_KEY:"anon",NEXT_PUBLIC_STOREFRONT_BASE_URL:"https://saltycowhide.com"})); if(!credentialReadiness.failures.includes("CREDENTIAL_ENCRYPTION_KEY required when encrypted credential storage is enabled")) failures.push("production readiness did not require credential encryption key"); try{selectRepositoryAdapter({APP_ENV:"production",NODE_ENV:"production",REPOSITORY_ADAPTER:"memory",DATABASE_URL:"postgres://example"}); failures.push("production allowed memory repository adapter");}catch(e){ if(!(e instanceof Error)||!e.message.includes("forbidden")) failures.push("production memory repository failure was unclear"); } const selected=selectRepositoryAdapter({APP_ENV:"production",NODE_ENV:"production",DATABASE_URL:"postgres://example"}); if(selected.adapter!=="drizzle") failures.push("production did not select Drizzle repositories"); if(env.includes("STUDIO_AUTH_ENABLED=false")) failures.push("production auth bypass documented"); const authText=readFileSync(join("packages","auth","src","index.ts"),"utf8"); const proxyText=readFileSync(join("apps","studio","proxy.ts"),"utf8"); const loginRoute=readFileSync(join("apps","studio","app","api","studio","login","route.ts"),"utf8"); if(!authText.includes("signInWithOtp")||!authText.includes("exchangeCodeForSession")||!authText.includes("getUser(accessToken)")) failures.push("Supabase Studio auth flow missing"); if(/Boolean\(process\.env\.STUDIO_ADMIN_EMAIL\)/.test(proxyText)||/return process\.env\.STUDIO_ADMIN_EMAIL\s*\?/.test(authText)||/createStudioSessionValue|verifyStudioSessionValue|sf_studio_session/.test(authText)) failures.push("production Studio auth is env-only or custom signed-cookie based"); if(/STUDIO_AUTH_SECRET|STUDIO_ADMIN_PASSWORD_HASH|PASSWORD_HASH|bcrypt|argon2/.test(env+authText)) failures.push("custom Studio auth secret/password workaround found"); if(/Set-Cookie/.test(loginRoute)) failures.push("Studio login route must not create sessions without Supabase callback"); if(!/pathname === "\/api\/studio\/login"/.test(proxyText)) failures.push("Studio login API is not explicitly public in proxy"); for(const p of ["publish/shopify","publish/printify"]){ const t=studioRoute(p); const post=t.split("export async function POST")[1]||""; if(!t.includes("requirePublishPermission")) failures.push(`${p}: publish route missing owner/admin publish permission`); if(/requireStudioUser\s*\(|requireAuditActor\s*\(/.test(t)) failures.push(`${p}: publish route uses generic auth`); if(!post.includes("evaluatePublishReviewGates")) failures.push(`${p}: publish POST gate evaluator missing`); if(/ok:\s*true/.test(post)) failures.push(`${p}: publish POST must not return fixture-only success`); if(!/not_implemented|provider_disabled/.test(post)) failures.push(`${p}: publish POST must fail honestly until persisted provider flow exists`); } const approveRoute=studioRoute("drafts/approve"); if(!approveRoute.includes("requireApprovalPermission")) failures.push("drafts/approve: approval route missing owner/admin permission"); if(/requireStudioUser\s*\(|requireAuditActor\s*\(/.test(approveRoute)) failures.push("drafts/approve: approval route uses generic auth"); for(const p of ["generate/submit","generate/status","mockups/generate","phrases/generate","trends/ingest"]){ const t=studioRoute(p); if(!t.includes("requireProviderMutationPermission")) failures.push(`${p}: provider-impacting route missing owner/admin permission`); } for(const routePath of walkRoutes(join("apps","studio","app","api","studio"))){ const t=readFileSync(routePath,"utf8"); if(/body\.(role|user_id|userId|organization_id|organizationId|workspace_id|workspaceId)/.test(t)) failures.push(`${routePath}: Studio API route appears to trust client-provided identity/workspace fields`); } const publicServiceRoleName="NEXT_PUBLIC_SUPABASE_"+"SERVICE_ROLE_KEY"; if((env+authText+proxyText).includes(publicServiceRoleName)) failures.push("Supabase service role key exposed as public"); if(failures.length){ console.error(failures.join("\n")); process.exit(1);} console.log("production checks passed with expected missing-secret readiness failures, Supabase Studio auth, route-level permissions, credential encryption readiness, and Drizzle repository enforcement");
